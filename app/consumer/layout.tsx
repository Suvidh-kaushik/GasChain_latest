import ConsumerWrapper from "@/components/layout/ConsumerWrapper"

export default function ConsumerLayoutRoot({ children }: { children: React.ReactNode }) {
  return <ConsumerWrapper>{children}</ConsumerWrapper>
}
